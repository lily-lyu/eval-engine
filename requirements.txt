jsonschema>=4.19.0
fastapi>=0.100.0
uvicorn>=0.22.0
pytest>=7.0.0
# Official MCP Python SDK (includes FastMCP); optional: mcp[cli] for dev tools
mcp>=1.0.0
# Gemini planner (v2); optional for deterministic-only deployment
google-generativeai>=0.8.0
